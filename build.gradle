version '0.1'

apply plugin: 'java'
apply plugin: 'eclipse'


sourceCompatibility = 1.8

jar {
    manifest {
        attributes "Main-Class": "sct.utxo.UtxoTool"
    }
 
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

allprojects {
	repositories {
			maven { url 'https://jitpack.io' }
	}
}

repositories {
    mavenCentral()
    maven {
    url 'http://mvn.neemre.com/'
    }

}

dependencies {
	//compile 'com.neemre.btcd-cli4j:btcd-cli4j-core:0.5.1'
			compile 'com.github.priiduneemre:btcd-cli4j:master'
	
}